source "https://rubygems.org"

# Minimal Gemfile for local Jekyll development
gem "jekyll", "~> 4.2"
# Needed for Ruby 3+ when running jekyll serve
gem "webrick", "~> 1.7"

# Optional: enable common plugins (uncomment if you want them)
# gem "jekyll-feed"

# If you prefer to reproduce GitHub Pages exactly, replace the above
# with: gem "github-pages", group: :jekyll_plugins
