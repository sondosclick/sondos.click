# Sondos Click

Hugo blog powered by the Blowfish theme.

## Local development

```bash
hugo server --buildDrafts
```

## Deploy

Push to `main` to deploy to GitHub Pages. The custom domain is set via `static/CNAME`.
